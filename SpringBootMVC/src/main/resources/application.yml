spring:
  application:
    name: spring-boot-mvc
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: none
  datasource:
    name: fileUpload
  http:
    multipart:
      max-file-size: 5000KB
      max-request-size: 5000KB
server:
  port: 8080